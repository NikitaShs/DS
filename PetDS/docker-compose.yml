services:
  postgres:           
    image: postgres
    environment:
      POSTGRES_DB: Nizhny
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: Nowgorod
    ports:
      - "134:5432"  
    volumes:
      - postgres-data:/var/lib/postgresql/data


volumes:
  postgres-data:
    